kind: Gateway
apiVersion: networking.istio.io/v1beta1
metadata:
  name: igw
  # namespace: app-cees-bcu-app
spec:
  servers:
    - port:
        number: 443
        protocol: HTTPS
        name: https
      hosts:
        - grpc-with-quarkus.app-cees-cs.freubel.sandbox2538.opentlc.com
      tls:
        mode: SIMPLE
        # mode: MUTUAL
        credentialName: igw-app-cees-cs-smcp
  selector:
    istio: ingressgateway

